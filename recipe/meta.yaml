{% set name = "pyquil" %}
{% set version = "2.0.0b8" %}
{% set sha256 = "9273eac714475c42fc495ff22a8514067b7aa43d4b555c3c8d90c4680b1be4da" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .
  entry_points:
    - pyquil-config-setup = pyquil.setup.pyquil_config_setup:main

requirements:
  build:
    - python
    - pip

  run:
    - python
    - numpy
    - six
    - antlr-python-runtime
    - requests
    - contextvars
    - networkx
    - dataclasses
    - rpcq

test:
  imports:
    - pyquil
    - pyquil.gates

about:
  home: http://forest.rigetti.com
  license: Apache-2.0
  license_family: Apache
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'
  summary: 'A Python library for quantum programming using Quil'
  doc_url: http://pyquil.readthedocs.io/en/latest/
  dev_url: https://github.com/rigetticomputing/pyquil

extra:
  recipe-maintainers:
    - mpharrigan
